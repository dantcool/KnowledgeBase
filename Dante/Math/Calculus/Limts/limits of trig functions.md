$lim_{x->0}\frac{sin(x)}{x} = 1$
$lim_{x->0}\frac{1-cosx}{x}=0$

example 
$lim_\theta->0 \frac{sin(2\theta)}{\theta}$

need our top and bottom to match so 
multiply top and bottom by top so $\frac{2}{2}$

giving us 

$lim_{\theta->0} \frac{sin(2\theta)}{2\theta} * 2$

1 * 2 = 2 our limit is 2 

example 
$lim_{h->\frac{\pi}{2}} \frac{1-cos(7h)}{h}$

plug in our limit $\pi/2$ we get 

$\frac{1-cos(\frac{7\pi}{2})}{\frac{\pi}{2}}$

find the value of $7\pi/2$
 we land on the -y axis and the coordinate are (0,1)
 cosine is the x coordinate meaning cos($7\pi/2)$ = 0
making our expression 

$\frac{1-0}{\frac{\pi}{2}} --> \frac{1}{\frac{\pi}{2}}$

dividing by a fraction means multiplying by its reciprocal flip our denominator and multiply by the top ultimately giving us 

$2/\pi$


Example
$lim_{x->0}\frac{x^2}{sin^2(7x)}$
plugging in 0 gives us 0/0 we use the special trig limit 

$lim_{\theta->0}\frac{\theta}{sin(\theta)} = 1$

so we rewite the expression first by handling the squares 
both our numeraator and denominator are squared so we canpull that exponent outside the fraction 

$(\frac{x}{sin(7x)})^2$

to use the tools our numerator must match the angle inside the sine function our angle is 7x and our top only had x so multiply by 7/7
giving us 

$(\frac{1}{7}*\frac{7x}{sin(7x)})^2$
our top and bottom ow match giving us 1 then multiply that by 1/7 leaving us 1/7 then just square the fraction giving us 

1/49


 